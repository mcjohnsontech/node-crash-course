// const {people, ages} = require('./people');

// console.log(`These people have the names of ${people} and ages of ${ages} respectively`);


const os = require('os');
console.log(os.platform(), os.homedir());